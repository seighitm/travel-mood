export enum RoleEnum {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum RelationshipStatusEnum {
  SINGLE = 'SINGLE',
  IN_RELATION = 'IN_RELATION',
}

export enum ACTIVATED_STATUS_ENUM {
  ACTIVATED = 'ACTIVATED',
  BLOCKED = 'BLOCKED',
}
